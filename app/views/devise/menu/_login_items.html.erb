<% if user_signed_in? %>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
      ACCOUNT
    <b class="caret"></b>
    </a>
    <ul class="dropdown-menu">
      <li>  <%= link_to("PROFILE", user_path(current_user) )%>  <li>
      <li>  <%= link_to "EDIT INFO", edit_user_registration_path %> </li>
      <li>  <%= link_to("FRIEND", relationships_path) %> </li>
      <li>  <%= link_to("REQUESTED FRIEND", requested_friends_relationships_path) %> </li>
      <li>  <%= link_to("REQUESTING FRIEND", requesting_friends_relationships_path) %> </li>
      <li>  <%= link_to("LOGOUT", destroy_user_session_path, method: :delete) %> </li>
    </ul>
  </li>
<% else %>
  <li>
  <%= link_to("LOGIN", new_user_session_path)  %>
  </li>
<% end %>
